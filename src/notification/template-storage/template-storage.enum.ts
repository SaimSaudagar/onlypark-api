export enum FileType {
  HTML = "HTML",
  JSON = "JSON",
  NONE = "NONE",
}

export enum TemplateType {
  EMAIL = "EMAIL",
  SMS = "SMS",
  PUSH = "PUSH",
}

export enum StorageType {
  FILE = "FILE",
  DATABASE = "DATABASE",
}
